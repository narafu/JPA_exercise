
# test-application.yml

### 주석 이유: springboot에서는 이렇게 안해도 된다. springboot에서는 db 설정이 없으면 자동으로 메모리를 사용한다.
#spring:
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 DB (H2)
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
